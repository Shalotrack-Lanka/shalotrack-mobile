<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- අලුතින් එකතු කළ Background Tracking Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".ShaloTrackApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LetsTrackLanka">

        <activity
            android:name=".ui.main.AlertsActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <activity
            android:name=".ui.main.CirclesActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <activity
            android:name=".ui.main.TagsActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <activity
            android:name=".ui.vehicles.VehiclesActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <activity android:name=".ui.vehicles.VehicleListActivity" android:exported="false" />

        <activity android:name=".ui.history.TripHistoryActivity" android:exported="false" />
        <activity android:name=".ui.history.TripDetailActivity" android:exported="false" />

        <activity android:name=".ui.contacts.EmergencyContactsActivity" android:exported="false" />

        <!-- FIX: was completely missing from the manifest despite being
             built and wired from the Settings bottom sheet several rounds
             ago, tapping "Notification & Alerts" would have crashed
             with ActivityNotFoundException, not just done nothing. Found
             during a systematic dead-end audit. -->
        <activity android:name=".ui.main.SettingsActivity" android:exported="false" />

        <!-- NEW: fabAdd in VehiclesActivity was a real, visible button
             with no destination at all until now (also found during the
             same audit). -->
        <activity android:name=".ui.vehicles.AddVehicleActivity" android:exported="false" />

        <!-- NEW: real Places screen, replacing the previous stub bottom
             sheet (just a close button, no actual functionality). -->
        <activity android:name=".ui.places.PlacesActivity" android:exported="false" />

        <!-- NEW: real Value stats screen, replacing the previous grayed
             "Coming soon" placeholder in the Vehicles tab grid menu. -->
        <activity android:name=".ui.vehicles.ValueActivity" android:exported="false" />

        <!-- NEW: Vehicle Sharing - pending invites and vehicles shared with you. -->
        <activity android:name=".ui.vehicles.VehicleSharingActivity" android:exported="false" />

        <!-- NEW: full live-tracking view for a vehicle shared with the
             viewer, reuses VehicleTrailRenderer directly for the same
             animated marker/heading experience owned vehicles get. -->
        <activity android:name=".ui.vehicles.SharedVehicleMapActivity" android:exported="false" />

        <!-- ============================================================ -->
        <!-- !! DO NOT ADD A LAUNCHER INTENT-FILTER TO HomeActivity !!      -->
        <!-- The launcher (MAIN/LAUNCHER) belongs on SplashActivity, below. -->
        <!-- HomeActivity is reached BY the splash screen navigating to it,-->
        <!-- not by launching directly. This has been reverted by a team   -->
        <!-- member at least twice already: if you're about to move the -->
        <!-- launcher here again, please ask first. Confirmed decision:    -->
        <!-- the splash screen must show on every app open.                -->
        <!-- ============================================================ -->
        <activity
            android:name=".ui.main.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <activity
            android:name=".ui.auth.ProcessingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.EmailInputActivity"
            android:launchMode="singleTask"
            android:exported="false" />

        <activity
            android:name=".ui.main.WelcomeActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <activity
            android:name=".ui.auth.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <activity
            android:name=".ui.auth.OtpVerificationActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />

        <!-- ============================================================ -->
        <!-- THE LAUNCHER LIVES HERE. This is the real app entry point.    -->
        <!-- Do not remove this intent-filter or move it elsewhere.       -->
        <!-- ============================================================ -->
        <activity
            android:name=".ui.main.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Link for Email Verification -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="auth.shalotrack.com" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <!-- Existing Firebase Messaging Service -->
        <service
            android:name=".services.ShaloTrackFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- අලුතින් එකතු කළ Background Real-time Tracking Service -->
        <service
            android:name=".services.TrackingForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCCEcm2yd65eBw5aUJI0akiXkwySTBS_LE" />

        <!-- Static API key is called in here for the GPS tracking history-->
        <meta-data android:name="com.example.letstracklanka.STATIC_MAPS_API_KEY"
            android:value="AIzaSyAAk1aVIma-49jfCJ40SDU-8fzufCGmQnA"/>
    </application>

</manifest>