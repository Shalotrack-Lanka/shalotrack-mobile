<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Bottom Navigation Bar (යටින්ම තියෙන මෙනුව) -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#FFFFFF"
        app:elevation="8dp" />

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottomNavigation">

        <!-- සම්පූර්ණ තිරය පුරාම තියෙන Map එක -->
        <fragment
            android:id="@+id/mapVehicles"
            android:name="com.google.android.gms.maps.SupportMapFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- දකුණු පැත්තේ පල්ලෙහා තියෙන + බටන් එක -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabAddDevice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="150dp"
            android:backgroundTint="#FFFFFF"
            android:src="@android:drawable/ic_input_add"
            app:tint="#1877F2" />

        <!-- අපි උඩින් හදපු Bottom Sheet එක මෙතනට සම්බන්ධ කරනවා -->
        <include layout="@layout/bottom_sheet_vehicle_realtime" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</RelativeLayout>